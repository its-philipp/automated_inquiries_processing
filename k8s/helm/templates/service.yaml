apiVersion: v1
kind: Service
metadata:
  name: {{ include "automated-inquiries-processing.fullname" . }}
  labels:
    {{- include "automated-inquiries-processing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "automated-inquiries-processing.selectorLabels" . | nindent 4 }}
